Todo:

- change the secret for generating the session keys

- reconfigure session configuration

- remove the user session when deleting their account

- Include more transports for logging

- Fix format error in function regist (user/controller.js)

- Find a good place to put permission levels for user schema key 'permission' and 'passport' middleware

- Configure static paths, make sure to place before session or passport

- Process various errors in model

- Respond with formatted errors instead of mongoDB or pre generated errors

- Come up with better response error codes and messages

- Get read/write access to newly created /data and /data/db folders in mac start up script

- Come up with API documentation for implemented paths
